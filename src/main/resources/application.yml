server:
  servlet:
    context-path: api

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:33306/timedeal
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 16379
